export enum UnitMeasure {
  UNIDADES = 'unidades',
  KG = 'kg',
  LITROS = 'litros',
}

export enum MovementType {
  IN = 'IN',
  OUT = 'OUT',
}

export enum MovementReason {
  COMPRA = 'compra',
  VENTA = 'venta',
  AJUSTE = 'ajuste',
  MERMA = 'merma',
  DEVOLUCION = 'devolución',
}
