DATABASE_URL=postgresql://inventario:inventario@localhost:5432/inventario
NODE_ENV=development
CORS_ORIGIN=http://localhost:8081

# Render: usar Internal Database URL en DATABASE_URL, NODE_ENV=production
# TypeORM crea tablas al arrancar (synchronize) y usa SSL en producción
